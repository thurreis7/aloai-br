---
phase: 07-promise-parity-and-launch-readiness
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/realtimeEvents.js
  - src/pages/Inbox.jsx
  - src/pages/Kanban.jsx
  - src/pages/Dashboard.jsx
  - src/pages/Team.jsx
  - src/hooks/useInboxNotifications.js
autonomous: true
requirements:
  - PROD-03
must_haves:
  truths:
    - D-05 requires `presence.updated` online/offline changes to appear within 2 seconds in Inbox and Team.
    - D-06 requires all six canonical events to be verified in Inbox, Kanban, and Dashboard.
    - D-07 requires the canonical realtime envelope on every canonical event.
    - D-08 requires envelope fields `event`, `workspace_id`, `resource_type`, `resource_id`, `actor_id`, `occurred_at`, `version`, and `payload`.
    - The implementation must close promise-parity gaps without adding new v1 product capabilities.
    - Realtime work must stay scoped to the SPEC event set: conversation.created, message.created, conversation.updated, assignment.updated, kanban.updated, presence.updated.
  artifacts:
    - src/lib/realtimeEvents.js
    - src/pages/Inbox.jsx
    - src/pages/Kanban.jsx
    - src/pages/Dashboard.jsx
    - src/pages/Team.jsx
    - src/hooks/useInboxNotifications.js
  key_links:
    - raw Supabase postgres_changes payloads must be normalized into the SPEC envelope before Phase 7 verification claims pass
    - Inbox needs visible presence behavior in addition to Team
    - Kanban and Dashboard need explicit realtime refresh behavior for release parity
---

<objective>
Close the product-behavior parity gap around realtime and presence so the operator-facing surfaces can demonstrate the SPEC v1 live workflow.

Purpose: make the six canonical realtime events and `presence.updated` acceptance concrete across Inbox, Kanban, Dashboard, and Team without broad architecture churn.
Output: a narrow realtime envelope helper plus page-level integrations that let the final smoke/UAT verify the locked event contract.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/SPEC.md
@.planning/design/api-contract.md
@.planning/design/03-realtime-map.md
@.planning/phases/07-promise-parity-and-launch-readiness/07-CONTEXT.md
@.planning/phases/07-promise-parity-and-launch-readiness/07-RESEARCH.md
@.planning/phases/07-promise-parity-and-launch-readiness/07-PATTERNS.md
@.planning/phases/06-operations-security-and-verification/06-CONTEXT.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Add a canonical realtime envelope adapter and validation helpers</name>
<read_first>
src/pages/Inbox.jsx
src/hooks/useInboxNotifications.js
src/pages/Team.jsx
.planning/SPEC.md
.planning/design/03-realtime-map.md
@.planning/phases/07-promise-parity-and-launch-readiness/07-CONTEXT.md
</read_first>
<files>
src/lib/realtimeEvents.js
</files>
<action>
Create a small shared helper that maps existing Supabase `postgres_changes` payloads into the canonical v1 realtime envelope. It must define the six allowed events, required envelope fields, resource mappings, and validation helpers usable by page code and smoke checks. Keep this adapter additive and compatible with current direct Supabase subscriptions.
</action>
<acceptance_criteria>
- Helper can produce envelopes for `conversation.created`, `message.created`, `conversation.updated`, `assignment.updated`, `kanban.updated`, and `presence.updated`
- Every produced envelope includes `event`, `workspace_id`, `resource_type`, `resource_id`, `actor_id`, `occurred_at`, `version`, and `payload`
- Invalid or unsupported events are rejected or ignored deterministically
- No additional realtime event types are introduced outside the SPEC v1 set
- Existing page-level subscription style remains intact
</acceptance_criteria>
<verify>
<automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/lib/realtimeEvents.js','utf8'); ['conversation.created','message.created','conversation.updated','assignment.updated','kanban.updated','presence.updated','workspace_id','resource_id','occurred_at','payload'].forEach(x=>{ if(!s.includes(x)) throw new Error('missing '+x) })"</automated>
</verify>
<done>
Canonical realtime envelopes can be generated and validated from the current Supabase realtime payload shape.
</done>
</task>

<task type="auto">
<name>Task 2: Wire canonical realtime handling into Inbox, notifications, Kanban, and Dashboard</name>
<read_first>
src/lib/realtimeEvents.js
src/pages/Inbox.jsx
src/hooks/useInboxNotifications.js
src/pages/Kanban.jsx
src/pages/Dashboard.jsx
@.planning/phases/07-promise-parity-and-launch-readiness/07-RESEARCH.md
</read_first>
<files>
src/pages/Inbox.jsx
src/hooks/useInboxNotifications.js
src/pages/Kanban.jsx
src/pages/Dashboard.jsx
</files>
<action>
Update the existing Supabase realtime subscriptions so each relevant table change is normalized through the canonical envelope helper before updating UI state. Ensure Inbox handles conversation/message updates, Kanban refreshes board state from conversation lifecycle events, and Dashboard refreshes operational metrics from conversation/message/assignment/kanban/presence-relevant changes. Keep behavior workspace-scoped and avoid introducing a global store.
</action>
<acceptance_criteria>
- Inbox still updates message timeline and conversation list from realtime changes
- Inbox/notifications use canonical `message.created`, `conversation.created`, and `conversation.updated` envelopes where applicable
- Kanban refreshes or updates board state for canonical `conversation.updated`, `assignment.updated`, and `kanban.updated`
- Dashboard refreshes operational metrics when canonical events materially affect displayed data
- Workspace scoping is preserved on every realtime-driven read/update
- No sales pipeline states or new lifecycle states are introduced
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
<automated>rg -n "conversation\\.created|message\\.created|conversation\\.updated|assignment\\.updated|kanban\\.updated|toRealtimeEnvelope|validateRealtimeEnvelope" src/lib src/pages src/hooks</automated>
</verify>
<done>
Inbox, Kanban, Dashboard, and notifications consume realtime changes through the canonical event contract.
</done>
</task>

<task type="auto">
<name>Task 3: Make presence.updated visible in Inbox and Team within the 2-second acceptance target</name>
<read_first>
src/lib/realtimeEvents.js
src/pages/Inbox.jsx
src/pages/Team.jsx
src/pages/Dashboard.jsx
@.planning/phases/07-promise-parity-and-launch-readiness/07-CONTEXT.md
</read_first>
<files>
src/pages/Inbox.jsx
src/pages/Team.jsx
src/pages/Dashboard.jsx
</files>
<action>
Wire presence-related membership/user changes through the canonical `presence.updated` envelope and expose online/offline status in Inbox and Team. Inbox should show a lightweight operational presence signal without creating a new page or presence product area. Team should preserve its existing member list behavior while using the canonical event path.
</action>
<acceptance_criteria>
- `presence.updated` is generated from workspace member/user online status changes
- Team continues to show online/offline state and refreshes from realtime changes
- Inbox exposes online/offline presence in an operator-relevant place, such as assignee/team availability context
- The target UAT can verify online/offline changes appear within 2 seconds in Inbox and Team
- Dashboard online counts remain consistent with the same source data
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
<automated>rg -n "presence\\.updated|is_online|online|offline" src/pages/Inbox.jsx src/pages/Team.jsx src/pages/Dashboard.jsx src/lib/realtimeEvents.js</automated>
</verify>
<done>
Presence is visible and testable in the two required surfaces using the canonical realtime event contract.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase realtime payload -> UI state | Raw table-change payloads must be normalized before being treated as canonical product events. |
| workspace events -> visible surfaces | Realtime changes must not update another workspace's Inbox, Kanban, Dashboard, or Team state. |
| presence source -> operator decisions | Online/offline state must be timely enough to support routing and workload decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | realtime event envelope | mitigate | validate canonical event names and required envelope fields before UI consumption |
| T-07-02 | I | workspace-scoped realtime updates | mitigate | preserve workspace filters and ignore events without matching `workspace_id` |
| T-07-03 | R | launch parity evidence | mitigate | make event handling traceable through smoke/UAT markers and canonical helper checks |
</threat_model>

<verification>
- Run `npm run build`
- Run marker checks for all six canonical events and envelope fields
- Manually verify presence appears in Inbox and Team within 2 seconds using guided UAT
- Confirm Kanban still uses only the six SPEC lifecycle states
- Confirm Dashboard metrics refresh after relevant realtime events
</verification>

<success_criteria>
- `PROD-03` is materially supported by realtime/presence behavior that matches the SPEC promise
- All six canonical realtime events are represented and verifiable
- `presence.updated` acceptance is testable in Inbox and Team
- No new v1 product scope is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-promise-parity-and-launch-readiness/07-01-SUMMARY.md`
</output>

