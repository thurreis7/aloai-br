---
phase: 02-unified-inbox-crm-workflow
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src/pages/Kanban.jsx
  - src/hooks/useInboxNotifications.js
  - src/pages/Inbox.jsx
  - src/lib/channels.js
autonomous: true
requirements:
  - INBX-01
  - INBX-02
  - INBX-03
  - INBX-04
  - INBX-05
must_haves:
  truths:
    - The kanban uses the six SPEC conversation states and no sales-pipeline labels.
    - Kanban movement updates the same conversation lifecycle vocabulary used by the inbox.
    - Realtime activity and notification copy use conversation-state semantics instead of pipeline semantics.
    - Instagram inbound remains visible in the queue, but its composer affordance stays disabled.
    - Workspace scoping and canonical channel metadata remain the only source of truth for visible cards and notifications.
  artifacts:
    - src/pages/Kanban.jsx
    - src/hooks/useInboxNotifications.js
    - src/pages/Inbox.jsx
    - src/lib/channels.js
  key_links:
    - kanban and inbox now share one conversation lifecycle vocabulary
    - realtime feedback reflects conversation updates rather than sales-stage language
    - canonical channel metadata remains the only display source for channel identity
---

<objective>
Normalize the operational surfaces around the canonical conversation lifecycle so the kanban, notifications, and inbox queue all speak the same six-state vocabulary.

Purpose: remove remaining sales-stage language from the workspace UI and keep Phase 2 aligned to the SPEC instead of the old pipeline model.
Output: kanban columns, queue filters, and realtime feedback all reflect the six approved conversation states.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
@.planning/phases/02-unified-inbox-crm-workflow/02-01-PLAN.md
@.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Normalize the kanban to the six SPEC lifecycle states</name>
<read_first>
src/pages/Kanban.jsx
src/hooks/usePermissions.jsx
src/hooks/useAuth.jsx
.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
</read_first>
<files>
src/pages/Kanban.jsx
</files>
<action>
Replace the current sales-oriented kanban columns with the six SPEC states: `new`, `open`, `ai_handling`, `human_handling`, `waiting_customer`, and `closed`. Keep the workspace-scoped Supabase read pattern, but make `conversation.state` the visual grouping key and use the backend write contract from Task 1 in the sibling plan for any state transitions. Remove the current negotiation/follow-up/resolved language from labels, empty states, and movement affordances.
</action>
<acceptance_criteria>
- Kanban columns are exactly the six SPEC states
- No sales-only column labels remain in the kanban UI
- Cards are grouped and moved by `conversation.state`
- Kanban still respects workspace scope and role permissions
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The kanban speaks the same conversation lifecycle vocabulary as the spec and no longer exposes a sales-pipeline surface.
</done>
</task>

<task type="auto">
<name>Task 2: Align realtime feedback and queue semantics</name>
<read_first>
src/hooks/useInboxNotifications.js
src/pages/Inbox.jsx
src/lib/channels.js
.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
</read_first>
<files>
src/hooks/useInboxNotifications.js
src/pages/Inbox.jsx
</files>
<action>
Update realtime notifications and any remaining queue filters or labels so they reference the canonical conversation states instead of pipeline language. Keep the existing Supabase realtime subscription model, but make the messaging and notification copy reflect inbox activity such as new messages and state changes rather than lead conversion or pipeline resolution. Preserve the instagram inbound read-only treatment and keep all channel identity derived from `src/lib/channels.js`.
</action>
<acceptance_criteria>
- Realtime notification copy no longer uses pipeline/resolution language
- Queue filters and labels match the six SPEC states
- Instagram inbound remains visible but read-only
- Workspace-scoped realtime updates continue to refresh the inbox without cross-workspace leakage
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
Realtime feedback and queue semantics now match the canonical conversation lifecycle instead of the old sales pipeline.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace conversation state -> kanban grouping | mismatched state labels can hide the true lifecycle of a conversation |
| Supabase realtime -> inbox notifications | stale or cross-workspace events must not leak into the active queue |
| channel metadata -> queue affordances | channel capability flags must control composer availability and visible actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `src/pages/Kanban.jsx` | mitigate | eliminate sales-stage columns and group solely by the approved conversation lifecycle |
| T-02-05 | I | `src/hooks/useInboxNotifications.js` | mitigate | keep notification copy tied to inbox events and preserve workspace-scoped realtime filtering |
| T-02-06 | E | `src/pages/Inbox.jsx` / `src/lib/channels.js` | mitigate | gate composer affordances by canonical channel capability, including read-only instagram inbound |
</threat_model>

<verification>
- Run `npm run build`
- Confirm the kanban column labels are the six SPEC states only
- Confirm notification copy no longer references sales-pipeline resolution
</verification>

<success_criteria>
- Kanban and inbox notification surfaces share the canonical conversation lifecycle
- Sales-stage vocabulary is removed from the active UI
- Instagram inbound remains read-only in the queue
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-inbox-crm-workflow/02-02-SUMMARY.md`
</output>
