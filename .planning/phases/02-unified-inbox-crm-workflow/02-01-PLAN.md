---
phase: 02-unified-inbox-crm-workflow
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260425_phase2_conversation_state_alignment.sql
  - alo-ai-api/src/controllers/conversation.controller.ts
  - alo-ai-api/src/services/conversation.service.ts
  - alo-ai-api/src/services/messaging.service.ts
  - src/pages/Inbox.jsx
  - src/lib/api.js
  - src/lib/channels.js
autonomous: true
requirements:
  - INBX-01
  - INBX-02
  - INBX-03
  - INBX-04
  - INBX-05
must_haves:
  truths:
    - The inbox list is read directly from workspace-scoped Supabase conversation, contact, and channel records.
    - `conversation.state` is canonical immediately, while `status` remains a compatibility alias maintained by trigger logic.
    - Mutation paths for message send, state transition, assignment, and close go through workspace-scoped backend endpoints.
    - The inbox composer uses optimistic updates for internal replies and backend-first dispatch for Evolution API sends.
    - Instagram inbound conversations remain visible in the queue, but the composer is disabled with an explanatory tooltip.
  artifacts:
    - supabase/migrations/20260425_phase2_conversation_state_alignment.sql
    - alo-ai-api/src/controllers/conversation.controller.ts
    - alo-ai-api/src/services/conversation.service.ts
    - alo-ai-api/src/services/messaging.service.ts
    - src/pages/Inbox.jsx
    - src/lib/api.js
    - src/lib/channels.js
  key_links:
    - direct Supabase reads keep inbox rendering local and workspace-scoped
    - backend writes enforce workspace boundaries and handle transport-specific sends
    - channel metadata and composer affordances are driven by canonical channel capabilities
---

<objective>
Implement the canonical inbox data and mutation contract for Phase 2 so conversation state, contact context, and message actions all align to the workspace-scoped spec.

Purpose: establish one inbox read model and one write path strategy before the kanban and notification surfaces are normalized.
Output: inbox data reads from Supabase directly, writes go through backend endpoints, and the state alias migration prevents `status` / `state` drift.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
@.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
@.planning/phases/01-tenant-and-channel-foundations/01-SECURITY.md
@.planning/design/api-contract.md
@.planning/SPEC.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Canonicalize conversation state and backend write endpoints</name>
<read_first>
supabase/migrations/20260424_phase1_workspace_foundations.sql
supabase/migrations/20260424_phase1_workspace_contract_alignment.sql
supabase/migrations/20260425_phase2_conversation_state_alignment.sql
alo-ai-api/src/controllers/compatibility.controller.ts
alo-ai-api/src/controllers/channel.controller.ts
alo-ai-api/src/controllers/workspace.controller.ts
alo-ai-api/src/services/messaging.service.ts
alo-ai-api/src/services/provisioning.service.ts
.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
</read_first>
<files>
supabase/migrations/20260425_phase2_conversation_state_alignment.sql
alo-ai-api/src/controllers/conversation.controller.ts
alo-ai-api/src/services/conversation.service.ts
alo-ai-api/src/services/messaging.service.ts
</files>
<action>
Add a new additive migration that makes `conversation.state` the canonical lifecycle field, keeps `status` in sync through trigger logic, and preserves workspace safety for all conversation/message tables touched by the inbox. Then add canonical workspace-scoped backend write endpoints for conversation message send, state transition, assignment, and close so the frontend can stop mutating those paths directly through Supabase. Keep the implementation additive: do not remove the existing brownfield compatibility fields, but do make the new canonical path explicit and enforce workspace scoping on every mutation.
</action>
<acceptance_criteria>
- The new migration exists and includes trigger or sync logic that keeps `state` canonical and `status` as an alias
- Backend exposes workspace-scoped write endpoints for message send, state transition, assignment, and close
- No write path depends on client-supplied `workspace_id` without backend verification
- Existing read compatibility is preserved for brownfield data
</acceptance_criteria>
<verify>
<automated>npm run build && node --check alo-ai-api/src/index.js</automated>
<automated>node -e "const fs=require('fs'); const p='supabase/migrations/20260425_phase2_conversation_state_alignment.sql'; const s=fs.readFileSync(p,'utf8'); ['create trigger','state','status'].forEach(x=>{ if(!s.includes(x)) throw new Error('missing '+x) })"</automated>
</verify>
<done>
Conversation state and message write behavior are canonicalized so the inbox can rely on a single workspace-scoped mutation contract.
</done>
</task>

<task type="auto">
<name>Task 2: Rewire inbox rendering and composer behavior</name>
<read_first>
src/pages/Inbox.jsx
src/lib/api.js
src/lib/channels.js
src/hooks/useAuth.jsx
src/hooks/usePermissions.jsx
.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
</read_first>
<files>
src/pages/Inbox.jsx
src/lib/api.js
src/lib/channels.js
</files>
<action>
Rebuild `src/pages/Inbox.jsx` around direct Supabase reads for the workspace-scoped queue and timeline, while routing writes through the backend contract introduced in Task 1. Keep the optimistic internal reply flow so agent messages feel immediate, but make Evolution sends backend-first and channel-aware. The inbox must show whatsapp, email, webchat, and inbound instagram conversations in one queue, with contact context, status, priority, and assignment state visible. Instagram inbound items stay read-only: the composer is disabled and explains why. Preserve realtime subscription behavior and scroll continuity, but remove any assumptions that unknown channels should collapse into WhatsApp.
</action>
<acceptance_criteria>
- Inbox list and thread data come from Supabase workspace-scoped reads
- Internal replies use optimistic UI before backend confirmation
- Evolution sends go through the backend first
- Instagram inbound rows are visible but the composer is disabled with a tooltip or equivalent explanation
- Channel labels and icons come from `src/lib/channels.js`
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The inbox behaves as one workspace-scoped queue with channel-aware composer rules and direct read paths.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase read model | workspace scoping must stay explicit on all inbox queries |
| browser -> backend write contract | state, assignment, and send actions must not trust client workspace claims |
| backend -> Evolution API | transport-specific send calls must stay isolated from inbox read logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | inbox Supabase reads | mitigate | keep every inbox query workspace-scoped and preserve RLS as the primary tenant barrier |
| T-02-02 | T | conversation state aliasing | mitigate | make `state` canonical and sync `status` via trigger so the UI and backend never diverge |
| T-02-03 | E | backend write endpoints | mitigate | verify workspace access server-side before any mutation or transport send occurs |
</threat_model>

<verification>
- Run `npm run build`
- Run `node --check alo-ai-api/src/index.js`
- Confirm the new migration contains state/status sync logic
- Confirm Instagram inbound remains visible in the queue while the composer is disabled
</verification>

<success_criteria>
- Inbox reads are Supabase-direct and workspace-scoped
- Backend handles write mutations and transport sends
- Conversation lifecycle uses `state` canonically with `status` compatibility preserved
- Instagram inbound is read-only in the queue
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-inbox-crm-workflow/02-01-SUMMARY.md`
</output>
</task>
</tasks>
