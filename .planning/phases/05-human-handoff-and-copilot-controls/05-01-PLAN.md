---
phase: 05-human-handoff-and-copilot-controls
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260428_phase5_handoff_and_copilot_controls.sql
  - alo-ai-api/src/controllers/conversation.controller.ts
  - alo-ai-api/src/controllers/ai-assist.controller.ts
  - alo-ai-api/src/services/conversation.service.ts
  - alo-ai-api/src/services/ai-assist.service.ts
  - alo-ai-api/src/app.module.ts
  - src/lib/api.js
  - src/pages/Inbox.jsx
  - src/hooks/usePermissions.jsx
autonomous: true
requirements:
  - HAND-01
  - HAND-02
must_haves:
  truths:
    - The canonical handoff lifecycle remains `ai_handling -> human_handling -> closed`.
    - No `handoff_pending` or `escalated` state is added in v1.
    - Human takeover pauses AI automatically at conversation level.
    - Copilot suggestions return only after explicit manual reactivation.
    - Handoff and takeover actions are auditable through existing audit infrastructure.
  artifacts:
    - supabase/migrations/20260428_phase5_handoff_and_copilot_controls.sql
    - alo-ai-api/src/services/conversation.service.ts
    - alo-ai-api/src/controllers/conversation.controller.ts
    - alo-ai-api/src/controllers/ai-assist.controller.ts
    - src/pages/Inbox.jsx
  key_links:
    - handoff controls must reuse the Phase 2 conversation lifecycle contract
    - copilot behavior must stay workspace-scoped and permission-gated
    - auditability must anchor on existing `audit_logs` usage patterns
---

<objective>
Build the explicit handoff and takeover contract so AI and human handling can switch safely inside existing conversation state transitions.

Purpose: make takeover and copilot behavior deterministic and operator-visible without introducing a new lifecycle taxonomy.
Output: one canonical backend handoff mutation path, one conversation-level copilot pause/resume contract, and one inbox control surface that reflects the current handoff mode.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/SPEC.md
@.planning/phases/05-human-handoff-and-copilot-controls/05-CONTEXT.md
@.planning/phases/05-human-handoff-and-copilot-controls/05-RESEARCH.md
@.planning/phases/04-intelligent-routing-and-lead-qualification/04-CONTEXT.md
@.planning/phases/03-workspace-ai-context-layer/03-CONTEXT.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Add additive handoff metadata and backend takeover or resume actions</name>
<read_first>
supabase/migrations/20260425_phase2_conversation_state_alignment.sql
alo-ai-api/src/controllers/conversation.controller.ts
alo-ai-api/src/controllers/ai-assist.controller.ts
alo-ai-api/src/services/conversation.service.ts
alo-ai-api/src/services/ai-assist.service.ts
alo-ai-api/src/services/access.service.ts
@.planning/phases/05-human-handoff-and-copilot-controls/05-CONTEXT.md
</read_first>
<files>
supabase/migrations/20260428_phase5_handoff_and_copilot_controls.sql
alo-ai-api/src/controllers/conversation.controller.ts
alo-ai-api/src/controllers/ai-assist.controller.ts
alo-ai-api/src/services/conversation.service.ts
alo-ai-api/src/services/ai-assist.service.ts
alo-ai-api/src/app.module.ts
</files>
<action>
Add an additive Phase 5 migration and backend contract that supports explicit human takeover and manual copilot reactivation per conversation. Reuse existing state vocabulary (`ai_handling`, `human_handling`, `closed`), persist conversation-level takeover or copilot flags, and record audit events in the existing audit surface on every takeover, pause, resume, and close transition.
</action>
<acceptance_criteria>
- Backend exposes workspace-scoped endpoints for human takeover and manual copilot reactivation
- Human takeover moves the conversation into `human_handling` and pauses AI behavior
- Copilot resumes only through explicit operator action and never by inactivity
- No new lifecycle state is introduced for pending handoff or escalation
- Handoff actions write canonical audit events tied to workspace and conversation
</acceptance_criteria>
<verify>
<automated>npm run build && node --check alo-ai-api/src/index.js</automated>
<automated>node -e "const fs=require('fs'); const p='supabase/migrations/20260428_phase5_handoff_and_copilot_controls.sql'; const s=fs.readFileSync(p,'utf8').toLowerCase(); ['handoff','copilot','audit'].forEach(x=>{ if(!s.includes(x)) throw new Error('missing '+x) })"</automated>
</verify>
<done>
The backend owns explicit takeover and manual reactivation with auditable events, while preserving the existing conversation lifecycle.
</done>
</task>

<task type="auto">
<name>Task 2: Surface handoff mode and control actions in Inbox with role gates</name>
<read_first>
src/pages/Inbox.jsx
src/lib/api.js
src/hooks/usePermissions.jsx
@.planning/phases/05-human-handoff-and-copilot-controls/05-CONTEXT.md
</read_first>
<files>
src/lib/api.js
src/pages/Inbox.jsx
src/hooks/usePermissions.jsx
</files>
<action>
Extend Inbox so operators can see and control handoff mode from the conversation workflow. Show explicit state for AI paused vs copilot active, expose takeover and resume actions only for allowed roles and scopes, and keep agent visibility limited to owned conversations while preserving current message send and routing behavior.
</action>
<acceptance_criteria>
- Inbox clearly indicates whether conversation is in AI handling or human handling mode
- Supervisor+ can perform takeover and reactivation actions from the conversation workflow
- Agent sees only own conversation handoff context and does not gain supervisor-only controls
- UI uses backend handoff endpoints; no client-only override logic
- Existing reply flow and routing features remain functional
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The inbox exposes explicit, permission-safe handoff controls and mode visibility on top of the existing workflow.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> handoff endpoints | clients can request takeover or resume, but backend must enforce workspace and role boundaries |
| conversation mutation -> AI assist behavior | takeover or resume state must be enforced server-side so UI cannot bypass pause rules |
| handoff actions -> audit infrastructure | audit events must remain complete and conversation-scoped for operational traceability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | E | takeover and resume endpoints | mitigate | enforce role-gated access and workspace membership checks before any mutation |
| T-05-02 | T | handoff lifecycle consistency | mitigate | allow only canonical state transitions and reject unsupported lifecycle values |
| T-05-03 | R | operator action traceability | mitigate | write audit events for takeover, AI pause, manual reactivation, and close |
</threat_model>

<verification>
- Run `npm run build`
- Run `node --check alo-ai-api/src/index.js`
- Confirm no new lifecycle states were introduced
- Confirm takeover pauses AI and manual resume is required
- Confirm handoff actions are auditable through the existing log surface
</verification>

<success_criteria>
- AI to human handoff is explicit, safe, and visible in Inbox
- Copilot pause and resume behavior is deterministic and manual
- Role and scope boundaries remain aligned with existing permission model
</success_criteria>

<output>
After completion, create `.planning/phases/05-human-handoff-and-copilot-controls/05-01-SUMMARY.md`
</output>
