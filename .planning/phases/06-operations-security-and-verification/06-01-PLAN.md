---
phase: 06-operations-security-and-verification
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260429_phase6_tenant_hardening_and_ops_metrics.sql
  - alo-ai-api/src/services/access.service.ts
  - alo-ai-api/src/controllers/conversation.controller.ts
  - alo-ai-api/src/services/conversation.service.ts
  - alo-ai-api/src/services/routing.service.ts
  - src/pages/Dashboard.jsx
  - src/pages/Kanban.jsx
autonomous: true
requirements:
  - HAND-04
  - PROD-01
must_haves:
  truths:
    - Manager monitoring in v1 includes queue backlog, unassigned conversations, escalated conversations, and AI-paused conversations.
    - Operational metrics are surfaced in existing `Dashboard` and `Kanban` views only.
    - Tenant hardening scope is backend + RLS only.
    - No dedicated ops health page is introduced in this phase.
    - No SLA metric implementation is required in v1.
  artifacts:
    - supabase/migrations/20260429_phase6_tenant_hardening_and_ops_metrics.sql
    - alo-ai-api/src/services/access.service.ts
    - src/pages/Dashboard.jsx
    - src/pages/Kanban.jsx
  key_links:
    - hardening must preserve workspace-scoped contracts established in Phase 1
    - ops metrics must consume canonical routing/handoff fields from Phases 4 and 5
    - backend access assertions and RLS policies must remain aligned
---

<objective>
Harden tenant safety and expose manager-grade operational state in existing surfaces so production supervision is possible without expanding product scope.

Purpose: ensure operational visibility and multitenant enforcement are reliable under real workflow load.
Output: one backend+RLS hardening pass and one operational metrics layer in Dashboard/Kanban for managers.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/SPEC.md
@.planning/phases/06-operations-security-and-verification/06-CONTEXT.md
@.planning/phases/06-operations-security-and-verification/06-RESEARCH.md
@.planning/phases/05-human-handoff-and-copilot-controls/05-CONTEXT.md
@.planning/phases/04-intelligent-routing-and-lead-qualification/04-CONTEXT.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Tighten backend and RLS tenant enforcement parity for critical mutations</name>
<read_first>
alo-ai-api/src/services/access.service.ts
alo-ai-api/src/controllers/conversation.controller.ts
alo-ai-api/src/services/conversation.service.ts
alo-ai-api/src/services/routing.service.ts
supabase/migrations/20260424_phase1_workspace_foundations.sql
supabase/migrations/20260419_owner_auth_multitenant.sql
@.planning/phases/06-operations-security-and-verification/06-CONTEXT.md
</read_first>
<files>
supabase/migrations/20260429_phase6_tenant_hardening_and_ops_metrics.sql
alo-ai-api/src/services/access.service.ts
alo-ai-api/src/controllers/conversation.controller.ts
alo-ai-api/src/services/conversation.service.ts
alo-ai-api/src/services/routing.service.ts
</files>
<action>
Implement a focused hardening pass that validates and tightens tenant-safe behavior across API access and RLS for the highest-risk conversation/routing/handoff mutations. Keep hardening backend+RLS only, add additive policy/index adjustments if needed, and preserve current workspace-scoped architecture.
</action>
<acceptance_criteria>
- Critical mutation paths enforce workspace membership and role-bound access consistently
- RLS coverage for critical tables remains active and compatible with API behavior
- Any policy/index changes are additive and do not break existing workspace contracts
- No frontend-only hardening layer is introduced in this phase
</acceptance_criteria>
<verify>
<automated>npm run build && node --check alo-ai-api/src/index.js</automated>
<automated>node -e "const fs=require('fs'); const p='supabase/migrations/20260429_phase6_tenant_hardening_and_ops_metrics.sql'; const s=fs.readFileSync(p,'utf8').toLowerCase(); ['workspace','policy','rls'].forEach(x=>{ if(!s.includes(x)) throw new Error('missing '+x) })"</automated>
</verify>
<done>
Tenant enforcement for critical paths is hardened with explicit parity between backend checks and RLS behavior.
</done>
</task>

<task type="auto">
<name>Task 2: Surface manager operational metrics in existing Dashboard and Kanban views</name>
<read_first>
src/pages/Dashboard.jsx
src/pages/Kanban.jsx
src/hooks/usePermissions.jsx
@.planning/phases/06-operations-security-and-verification/06-CONTEXT.md
</read_first>
<files>
src/pages/Dashboard.jsx
src/pages/Kanban.jsx
</files>
<action>
Extend existing manager views to expose the locked v1 operational set: queue backlog, unassigned conversations, escalated conversations, and AI-paused conversations. Keep metrics derived from canonical backend/Supabase data and avoid creating a dedicated ops health page or SLA module.
</action>
<acceptance_criteria>
- Dashboard exposes queue backlog and manager triage signals from canonical data
- Kanban exposes unassigned/escalated/AI-paused operational context without changing lifecycle semantics
- Operational indicators are workspace-scoped and role-appropriate
- No new dedicated ops view/page is introduced
- No SLA metric implementation is added in v1
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
Managers can supervise operational load and risk signals from existing views without scope expansion.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> critical mutation endpoints | API must enforce tenant and role checks regardless of client behavior |
| API access checks -> Supabase RLS | backend assertions and RLS policies must agree on allowed workspace scope |
| manager metrics UI -> source tables | operational indicators must derive from canonical workspace data, not local cached assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | E | critical conversation/routing/handoff mutations | mitigate | enforce role and workspace checks in API seams and verify policy parity |
| T-06-02 | I | multitenant query boundaries | mitigate | validate RLS coverage and workspace filters on operational reads |
| T-06-03 | T | manager ops metrics correctness | mitigate | compute metrics from canonical conversation/routing/handoff fields only |
</threat_model>

<verification>
- Run `npm run build`
- Run `node --check alo-ai-api/src/index.js`
- Confirm backend+RLS parity on critical mutation paths
- Confirm Dashboard/Kanban expose the locked manager operational set
- Confirm no new dedicated ops page and no SLA module were added
</verification>

<success_criteria>
- `HAND-04` and `PROD-01` are materially satisfied through hardened behavior and manager visibility
- Operational supervision works through existing surfaces only
- Tenant safety is demonstrably stronger without architecture churn
</success_criteria>

<output>
After completion, create `.planning/phases/06-operations-security-and-verification/06-01-SUMMARY.md`
</output>
