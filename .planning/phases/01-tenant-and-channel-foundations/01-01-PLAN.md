---
phase: 01-tenant-and-channel-foundations
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/access.js
  - src/hooks/useAuth.jsx
  - src/hooks/usePermissions.jsx
  - src/pages/Settings.jsx
  - src/pages/Team.jsx
  - supabase/migrations/20260424_phase1_workspace_contract_alignment.sql
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - Users only resolve and operate inside authorized workspace scope after sign-in.
    - Owner workspace switching uses the same tenant contract as workspace settings and team membership views.
    - Brownfield compatibility with workspaces, companies, workspace_users, workspace_members, and users.company_id is explicit instead of ad hoc.
  artifacts:
    - src/lib/access.js
    - src/hooks/useAuth.jsx
    - src/hooks/usePermissions.jsx
    - src/pages/Settings.jsx
    - src/pages/Team.jsx
    - supabase/migrations/20260424_phase1_workspace_contract_alignment.sql
  key_links:
    - resolveAuthAccess contract is consumed consistently by useAuth, usePermissions, Settings, and Team
    - migration compatibility rules support the runtime tenant contract instead of contradicting it
---

<objective>
Stabilize the workspace access contract across the brownfield frontend and Supabase tenant model so later multichannel work is built on one consistent workspace boundary.

Purpose: eliminate tenant drift across access resolution, permissions, settings, and team membership views.
Output: aligned tenant access code in the frontend plus a new compatibility migration for workspace contract alignment.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
@.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
@.planning/phases/01-tenant-and-channel-foundations/01-PATTERNS.md
@.planning/phases/01-tenant-and-channel-foundations/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Normalize the runtime workspace access contract</name>
<read_first>
src/lib/access.js
src/hooks/useAuth.jsx
src/hooks/usePermissions.jsx
src/pages/Settings.jsx
src/pages/Team.jsx
.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
</read_first>
<files>
src/lib/access.js
src/hooks/useAuth.jsx
src/hooks/usePermissions.jsx
src/pages/Settings.jsx
src/pages/Team.jsx
</files>
<action>
Implement D-01, D-03, D-05, and D-06 by making `src/lib/access.js` the explicit workspace-first compatibility layer for runtime tenant resolution. Keep read compatibility for `workspaces`, `companies`, and `users.company_id`, but add explicit membership fallback support for `workspace_users` and `workspace_members` so `resolveAuthAccess` returns one stable contract: `profile`, `memberships`, `workspaces`, `activeWorkspace`, `activeWorkspaceId`, `activeMembership`, `isOwner`, and `role`. Update `src/hooks/useAuth.jsx` to trust that contract instead of reconstructing tenant state. Update `src/hooks/usePermissions.jsx` so scoped permission lookup uses `profile.company_id || wsRole.workspace_id` consistently with owner/admin bypass unchanged. Update `src/pages/Settings.jsx` and `src/pages/Team.jsx` so workspace editing, workspace switching, and member loading all follow the same workspace-first fallback order and do not depend on contradictory assumptions about `workspace_members` vs `users.company_id`.
</action>
<acceptance_criteria>
- `src/lib/access.js` contains explicit fallback logic for both `workspace_users` and `workspace_members`
- `src/lib/access.js` still reads `workspaces` before `companies`
- `src/hooks/useAuth.jsx` consumes `resolveAuthAccess(` for tenant state and does not add a second tenant resolution path
- `src/hooks/usePermissions.jsx` resolves scoped permissions from `company_id` or `workspace_id` consistently
- `src/pages/Settings.jsx` and `src/pages/Team.jsx` both derive workspace-scoped behavior from `ws` / `wsRole` instead of introducing new tenant keys
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The authenticated frontend uses one consistent workspace contract across access, permissions, settings, and team membership flows without removing brownfield compatibility fallbacks.
</done>
</task>

<task type="auto">
<name>Task 2: Add a compatibility migration for the workspace contract</name>
<read_first>
supabase/migrations/20260419_owner_auth_multitenant.sql
src/lib/access.js
src/pages/Team.jsx
.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
</read_first>
<files>
supabase/migrations/20260424_phase1_workspace_contract_alignment.sql
</files>
<action>
Create a new migration file `supabase/migrations/20260424_phase1_workspace_contract_alignment.sql` instead of editing the prior applied migration in place. The SQL should document and implement the brownfield compatibility alignment required by D-01 and D-05: preserve workspace-first semantics, support both `workspace_users` and `workspace_members` where runtime code depends on them, and ensure tenant-scoped tables continue to enforce workspace-safe access. The migration must be additive and safe for an existing environment; do not delete legacy structures in this phase. Include comments that state the canonical outward vocabulary is `workspace` and that compatibility exists only to bridge brownfield drift.
</action>
<acceptance_criteria>
- `supabase/migrations/20260424_phase1_workspace_contract_alignment.sql` exists
- the migration file contains the string `workspace_members`
- the migration file contains the string `workspace_users`
- the migration file contains the string `canonical outward vocabulary is workspace`
- the migration file does not modify `20260419_owner_auth_multitenant.sql`
</acceptance_criteria>
<verify>
<automated>node -e "const fs=require('fs'); const p='supabase/migrations/20260424_phase1_workspace_contract_alignment.sql'; const s=fs.readFileSync(p,'utf8'); ['workspace_members','workspace_users','canonical outward vocabulary is workspace'].forEach(x=>{ if(!s.includes(x)) throw new Error('missing '+x) })"</automated>
</verify>
<done>
A new additive migration exists that aligns runtime tenant expectations with the brownfield schema direction without rewriting existing migration history.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase | authenticated user reads workspace-scoped data here |
| browser -> access helpers | wrong tenant fallback can leak another workspace's state here |
| migration -> live database | incorrect compatibility SQL can broaden access or break runtime assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | `src/lib/access.js` | mitigate | keep workspace-scoped fallback order explicit and never expand non-owner workspace lists |
| T-01-02 | E | `src/hooks/usePermissions.jsx` | mitigate | preserve owner/admin bypass only; all other roles stay scoped through workspace/company keys |
| T-01-03 | T | `supabase/migrations/20260424_phase1_workspace_contract_alignment.sql` | mitigate | make migration additive only and avoid destructive tenant-structure rewrites |
</threat_model>

<verification>
- Run `npm run build`
- Verify the new migration file exists and contains the explicit compatibility markers from this plan
- Spot-check that the updated access files still use workspace-scoped identifiers rather than introducing a new global tenant path
</verification>

<success_criteria>
- Phase 1 tenant access files use one explicit workspace contract
- A new compatibility migration exists for workspace contract alignment
- No new tenant-resolution path bypasses the workspace boundary
</success_criteria>

<output>
After completion, create `.planning/phases/01-tenant-and-channel-foundations/01-01-SUMMARY.md`
</output>
