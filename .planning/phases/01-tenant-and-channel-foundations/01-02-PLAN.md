---
phase: 01-tenant-and-channel-foundations
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src/lib/channels.js
  - src/pages/Channels.jsx
  - src/pages/Inbox.jsx
  - src/pages/Dashboard.jsx
  - alo-ai-api/src/index.js
  - supabase/functions/setup-workspace/index.js
  - supabase/functions/webhook-whatsapp/index.js
autonomous: true
requirements:
  - CHAN-01
  - CHAN-02
  - CHAN-03
  - CHAN-04
  - CHAN-05
must_haves:
  truths:
    - The product uses one canonical channel type set: whatsapp, instagram, email, and webchat.
    - Channel setup and operational surfaces render real workspace channel status instead of mismatched placeholder type names.
    - Backend and Supabase provisioning and WhatsApp ingestion honor the same workspace channel contract the frontend uses.
  artifacts:
    - src/lib/channels.js
    - src/pages/Channels.jsx
    - src/pages/Inbox.jsx
    - src/pages/Dashboard.jsx
    - alo-ai-api/src/index.js
    - supabase/functions/setup-workspace/index.js
    - supabase/functions/webhook-whatsapp/index.js
  key_links:
    - canonical channel definitions are shared by frontend operational pages
    - workspace setup writes channel types that the frontend and webhook handlers read without remapping drift
    - inbox and dashboard derive labels/counts from workspace channel records using canonical types
---

<objective>
Canonicalize the channel contract across the brownfield frontend, backend, and Supabase handlers so WhatsApp, Instagram, email, and webchat are represented consistently and visibly as real workspace channels.

Purpose: remove drift like `gmail` vs `email` and make channel status surfaces trustworthy enough for later unified inbox work.
Output: shared frontend channel metadata, aligned server-side channel writes, and real channel-aware inbox/dashboard rendering.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
@.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
@.planning/phases/01-tenant-and-channel-foundations/01-PATTERNS.md
@.planning/phases/01-tenant-and-channel-foundations/01-VALIDATION.md
@.planning/phases/01-tenant-and-channel-foundations/01-01-PLAN.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Canonicalize channel types across frontend and provisioning paths</name>
<read_first>
src/pages/Channels.jsx
src/pages/Inbox.jsx
src/pages/Dashboard.jsx
alo-ai-api/src/index.js
supabase/functions/setup-workspace/index.js
supabase/functions/webhook-whatsapp/index.js
.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
</read_first>
<files>
src/lib/channels.js
src/pages/Channels.jsx
alo-ai-api/src/index.js
supabase/functions/setup-workspace/index.js
supabase/functions/webhook-whatsapp/index.js
</files>
<action>
Implement D-02, D-03, D-04, and D-06 by creating `src/lib/channels.js` as the canonical frontend channel registry for exactly `whatsapp`, `instagram`, `email`, and `webchat`. Update `src/pages/Channels.jsx` to use that shared registry, replace the current `gmail` type with `email`, and keep brownfield read compatibility for legacy values by normalizing `gmail` to `email` during load or save when necessary. Update `alo-ai-api/src/index.js` and `supabase/functions/setup-workspace/index.js` so workspace setup and channel creation write the same canonical type values and human-readable names. Keep `supabase/functions/webhook-whatsapp/index.js` WhatsApp-specific, but ensure its channel lookup continues to operate inside the same canonical workspace channel contract.
</action>
<acceptance_criteria>
- `src/lib/channels.js` exists
- `src/lib/channels.js` contains `whatsapp`, `instagram`, `email`, and `webchat`
- `src/pages/Channels.jsx` does not contain the string `type: 'gmail'`
- `alo-ai-api/src/index.js` contains canonical channel handling for `email`
- `supabase/functions/setup-workspace/index.js` still provisions `email` and does not introduce `gmail`
</acceptance_criteria>
<verify>
<automated>npm run build && node --check alo-ai-api/src/index.js</automated>
</verify>
<done>
Frontend and provisioning flows use one canonical channel vocabulary and preserve brownfield compatibility where legacy values may still exist.
</done>
</task>

<task type="auto">
<name>Task 2: Reflect real channel status in inbox and dashboard surfaces</name>
<read_first>
src/lib/channels.js
src/pages/Channels.jsx
src/pages/Inbox.jsx
src/pages/Dashboard.jsx
alo-ai-api/src/index.js
.planning/phases/01-tenant-and-channel-foundations/01-CONTEXT.md
.planning/phases/01-tenant-and-channel-foundations/01-RESEARCH.md
</read_first>
<files>
src/pages/Channels.jsx
src/pages/Inbox.jsx
src/pages/Dashboard.jsx
</files>
<action>
Update `src/pages/Inbox.jsx` and `src/pages/Dashboard.jsx` to consume the canonical channel metadata from `src/lib/channels.js` and real workspace `channels` table values rather than assuming unknown data should default to WhatsApp. Ensure channel labels, colors, and status summaries render correctly for `whatsapp`, `instagram`, `email`, and `webchat`. Keep the existing page layouts and brownfield query style, but make the displayed connection/volume state depend on real workspace records and canonical type normalization.
</action>
<acceptance_criteria>
- `src/pages/Inbox.jsx` resolves channel labels from canonical metadata and does not default unknown channel types to `whatsapp`
- `src/pages/Dashboard.jsx` derives channel colors/counts from canonical channel types
- `src/pages/Channels.jsx` displays status chips for canonical types `whatsapp`, `instagram`, `email`, and `webchat`
- the frontend build succeeds after the canonical channel module is wired into all three pages
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The channel-facing product surfaces render canonical types and real workspace channel status consistently enough to support later unified inbox work.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> workspace channel records | wrong type normalization can hide or misroute channel state here |
| backend setup -> channels table | inconsistent write values can break downstream UI and webhook reads here |
| webhook input -> channel lookup | untrusted inbound data crosses into workspace channel selection here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | `src/pages/Channels.jsx` | mitigate | normalize legacy channel values explicitly and render only canonical outward types |
| T-01-04 | I | `src/pages/Inbox.jsx` / `src/pages/Dashboard.jsx` | mitigate | stop defaulting unknown types to WhatsApp and derive labels/colors from canonical metadata |
| T-01-05 | S | `supabase/functions/webhook-whatsapp/index.js` | mitigate | preserve workspace-scoped channel lookup and do not widen inbound lookup beyond the current workspace channel contract |
</threat_model>

<verification>
- Run `npm run build`
- Run `node --check alo-ai-api/src/index.js`
- Confirm the canonical channel module exists and the `gmail` UI token has been removed from `src/pages/Channels.jsx`
</verification>

<success_criteria>
- Canonical channel types are shared across frontend and provisioning paths
- Inbox and dashboard surfaces reflect real workspace channel status using canonical metadata
- Phase 1 finishes with no remaining `gmail` vs `email` contract drift in the active product path
</success_criteria>

<output>
After completion, create `.planning/phases/01-tenant-and-channel-foundations/01-02-SUMMARY.md`
</output>
