---
phase: 03-workspace-ai-context-layer
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src/pages/Knowledge.jsx
  - src/pages/Automation.jsx
  - src/pages/Inbox.jsx
  - src/lib/api.js
  - src/lib/channels.js
  - src/hooks/usePermissions.jsx
autonomous: true
requirements:
  - AICX-01
  - AICX-02
  - AICX-03
must_haves:
  truths:
    - Knowledge editing persists workspace context, FAQ entries, and file references through the backend contract.
    - Automation becomes the canonical admin surface for AI enablement, tone, threshold, and channel or schedule policy.
    - Inbox suggestion generation is on-demand and workspace-scoped.
    - Channel and schedule policy affect suggestion availability only in Phase 3.
    - `owner` and `admin` can edit AI policy, while `supervisor` and `agent` can only consume suggestions in Inbox.
  artifacts:
    - src/pages/Knowledge.jsx
    - src/pages/Automation.jsx
    - src/pages/Inbox.jsx
    - src/lib/api.js
    - src/lib/channels.js
  key_links:
    - the UI should consume the persistent workspace AI config instead of local storage
    - suggestion behavior must remain assistive and on-demand
    - channel capability rules should drive availability without introducing auto-send rights
---

<objective>
Rewire the visible AI-related surfaces so workspace admins manage persistent AI context in the UI and Inbox operators consume on-demand suggestions from the same workspace contract.

Purpose: remove local placeholder state from the Knowledge and Automation pages and connect Inbox suggestions to the backend context model.
Output: one persistent admin workflow for AI context and one workspace-scoped suggestion path in Inbox.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/SPEC.md
@.planning/design/api-contract.md
@.planning/design/02-api-contract.md
@.planning/phases/03-workspace-ai-context-layer/03-CONTEXT.md
@.planning/phases/03-workspace-ai-context-layer/03-01-PLAN.md
@.planning/phases/02-unified-inbox-crm-workflow/02-CONTEXT.md
@.planning/phases/02-unified-inbox-crm-workflow/02-VERIFICATION.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Persist workspace AI knowledge and policy in the admin surfaces</name>
<read_first>
src/pages/Knowledge.jsx
src/pages/Automation.jsx
src/lib/api.js
src/hooks/usePermissions.jsx
src/hooks/useAuth.jsx
@.planning/phases/03-workspace-ai-context-layer/03-CONTEXT.md
</read_first>
<files>
src/pages/Knowledge.jsx
src/pages/Automation.jsx
src/lib/api.js
</files>
<action>
Replace the localStorage-backed behavior in `Knowledge.jsx` and `Automation.jsx` with backend reads and writes against the new workspace AI context contract. Keep the same conceptual three-layer model: company context text, editable FAQ entries, and uploaded reference files. Make `Knowledge.jsx` the workspace knowledge editor and `Automation.jsx` the policy editor for enablement, tone, confidence threshold, channel allow/deny rules, and schedule rules. Preserve role gating so only owner and admin can modify the config.
</action>
<acceptance_criteria>
- Knowledge edits persist beyond local browser state
- Automation edits persist through the backend AI config contract
- Uploaded files stay in Supabase Storage with metadata references saved in the workspace AI config
- Non-admin roles cannot edit AI policy
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
The admin AI surfaces now persist workspace context instead of pretending local state is shared configuration.
</done>
</task>

<task type="auto">
<name>Task 2: Wire on-demand workspace suggestions into Inbox</name>
<read_first>
src/pages/Inbox.jsx
src/lib/api.js
src/lib/channels.js
src/hooks/usePermissions.jsx
@.planning/phases/03-workspace-ai-context-layer/03-CONTEXT.md
</read_first>
<files>
src/pages/Inbox.jsx
src/lib/api.js
src/lib/channels.js
</files>
<action>
Replace the fake local AI suggestion logic in `Inbox.jsx` with an on-demand backend call that uses the active workspace AI context. Keep the suggestion bar assistive, not autonomous, and make it respect workspace and channel policy gates. Channel and schedule rules should affect whether a suggestion is available, but they must not create implicit auto-send rights. Continue to respect the existing instagram inbound read-only behavior and keep the composer disabled there.
</action>
<acceptance_criteria>
- Inbox suggestions are requested on demand from the backend
- Suggestion availability is scoped to the active workspace AI config
- Channel and schedule policy affect suggestions but do not enable auto-send
- Instagram inbound remains visible and read-only
</acceptance_criteria>
<verify>
<automated>npm run build</automated>
</verify>
<done>
Inbox suggestions now consume the same workspace AI context that admins manage, without turning the UI into an autonomous sender.
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> backend suggestion request | suggestion generation must verify workspace membership and role policy server-side |
| admin UI -> workspace AI config | only owner and admin may mutate the context or policy |
| inbox suggestion response -> composer | suggested text must remain a manual assist, not an automatic outbound send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | E | inbox suggestion endpoint | mitigate | require workspace-scoped verification before generating or returning suggestions |
| T-03-05 | I | knowledge and automation UIs | mitigate | remove local placeholder state and bind edits to the backend contract |
| T-03-06 | T | policy-gated suggestion availability | mitigate | keep channel and schedule policy read-only in Inbox and never infer send rights from it |
</threat_model>

<verification>
- Run `npm run build`
- Confirm Knowledge and Automation persist data through the backend
- Confirm Inbox suggestions are on-demand and workspace-scoped
- Confirm Instagram inbound remains read-only
</verification>

<success_criteria>
- Workspace AI context is editable from the UI and persisted in the backend
- Inbox suggestions consume the active workspace AI context instead of local placeholders
- Channel and schedule policy only change suggestion availability, not send rights
</success_criteria>

<output>
After completion, create `.planning/phases/03-workspace-ai-context-layer/03-02-SUMMARY.md`
</output>
</task>
</tasks>
